# Seed content

Example pages, articles, and site settings. Import with:

```bash
pnpm sanity:dataset-import
```

After importing, open `/studio` to edit content, or visit `/` to see the homepage.

**Note:** Seed data uses the new schema shape (`page` with URI, `site` singleton, `article` documents). Re-import replaces existing documents in the target dataset.
